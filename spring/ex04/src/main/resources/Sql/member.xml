<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">
    <insert id="insert">
        insert into member (email, name)
        values ('아아아@naver.com','aaa')
        <!--values (#{email}, #{name})
        ON DUPLICATE KEY UPDATE name = #{name};
    --></insert>
</mapper>
